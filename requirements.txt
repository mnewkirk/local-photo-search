# Force CPU-only PyTorch wheels — avoids pulling 2+ GB of NVIDIA CUDA libs
# on CPU-only hardware (e.g. Intel N100 NAS). Must come before open-clip-torch.
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# Core
click>=8.0
numpy>=1.20
pyyaml>=6.0

# Image handling
Pillow>=10.0
pillow-heif>=0.18     # HEIC / HEIF support for iPhone/iPad photos
opencv-python>=4.5    # Used by InsightFace for image processing
exifread>=3.0         # EXIF extraction (handles HEIC, JPEG, TIFF, ARW)

# Embeddings & search
open-clip-torch>=2.24
sqlite-vec>=0.1
scipy>=1.10              # Hierarchical clustering for shoot review
scikit-learn>=1.3        # DBSCAN for face reclustering

# Face recognition (InsightFace + ArcFace)
insightface>=0.7
onnxruntime>=1.16     # Runtime for InsightFace models

# Scene descriptions (via Ollama)
ollama>=0.3

# Color extraction
colorthief>=0.2.1

# Reverse geocoding (offline)
reverse_geocoder>=1.5

# Web UI
fastapi>=0.110
uvicorn>=0.29

# Google Photos upload (optional — only needed if using the upload feature)
# requests is already transitively required by many packages; listed explicitly
# so Docker image builds don't break if transitive dep is removed.
requests>=2.31

# Testing
pytest>=8.0
pytest-cov>=5.0
